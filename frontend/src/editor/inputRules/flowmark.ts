import { InputRule } from "prosemirror-inputrules";
import { Schema } from "prosemirror-model";

/**
 * Builds FlowMark input rules.
 * 
 * This is a placeholder for future FlowMark-specific input rules.
 * FlowMark rules will be layered on top of Markdown rules.
 * 
 * @param schema - The ProseMirror schema
 * @returns Array of FlowMark input rules (currently empty)
 */
export function buildFlowMarkInputRules(schema: Schema): InputRule[] {
  // Placeholder for future FlowMark rules
  // FlowMark rules will be added here as needed
  return [];
}

